# C# JSON Test

This project demonstrates JSON serialization and deserialization in C# using a .NET Console Application and the [Newtonsoft.Json](https://www.newtonsoft.com/json) (Json.NET) library.

## How to run

Ensure you have the [.NET SDK](https://dotnet.microsoft.com/download) installed, then run:

```bash
dotnet run
```
