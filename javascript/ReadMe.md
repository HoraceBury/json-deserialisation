# JavaScript JSON Test

This project demonstrates JSON serialization and deserialization in JavaScript using the native JSON object.

## How to run

Ensure you have [Node.js](https://nodejs.org/) installed, then run:

```bash
node index.js
```
